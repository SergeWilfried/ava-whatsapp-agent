GROQ_API_KEY=""

ELEVENLABS_API_KEY=""
ELEVENLABS_VOICE_ID=""

TOGETHER_API_KEY=""

QDRANT_URL=""
QDRANT_API_KEY=""

# WhatsApp Configuration (Fallback - used if business lookup fails)
WHATSAPP_PHONE_NUMBER_ID = ""
WHATSAPP_TOKEN = ""
WHATSAPP_VERIFY_TOKEN = ""

# MongoDB Configuration (for multi-business support)
MONGODB_URI=""
MONGODB_DB_NAME=""
ENCRYPTION_SECRET=""

# Production Performance Settings
BUSINESS_CACHE_TTL="300"  # Cache TTL in seconds (5 minutes)
BUSINESS_CACHE_SIZE="1000"  # Maximum cached businesses
MONGODB_MAX_POOL_SIZE="100"  # MongoDB connection pool max size
MONGODB_MIN_POOL_SIZE="10"   # MongoDB connection pool min size
WARMUP_CACHE="true"  # Warm up cache on startup

# Optional: Redis for distributed caching (multi-instance deployments)
# REDIS_URL="redis://localhost:6379/0"

# CartaAI API Configuration
# Master switch to enable CartaAI API integration (set to "true" to use API instead of mock data)
USE_CARTAAI_API="false"

# API Credentials (REQUIRED if USE_CARTAAI_API=true)
# Note: Base URL should NOT include /api/v1 path - it's added automatically by the services
CARTAAI_API_BASE_URL="https://ssgg.api.cartaai.pe"
CARTAAI_SUBDOMAIN=""  # Your restaurant subdomain
CARTAAI_LOCAL_ID=""   # Your restaurant local ID
CARTAAI_API_KEY=""    # Your API key

# Feature Flags (enable specific API endpoints)
CARTAAI_MENU_ENABLED="false"     # Enable menu API (categories, products)
CARTAAI_ORDERS_ENABLED="false"   # Enable orders API (create orders)
CARTAAI_DELIVERY_ENABLED="false" # Enable delivery API (tracking)

# Performance Settings
CARTAAI_TIMEOUT="30"                    # Request timeout in seconds
CARTAAI_MAX_RETRIES="3"                 # Maximum retry attempts
CARTAAI_RETRY_DELAY="1.0"               # Initial retry delay in seconds
CARTAAI_CACHE_TTL="900"                 # Cache TTL in seconds (15 minutes)
CARTAAI_ENABLE_CACHE="true"             # Enable caching
CARTAAI_MAX_CONCURRENT_REQUESTS="10"    # Max parallel requests

# Logging
ENABLE_API_LOGGING="false"  # Enable detailed API request/response logging
